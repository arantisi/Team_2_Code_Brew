version: 0.2

phases:
  runtime-versions:
    java: openjdk8
  build:
    commands:
      - echo Entered the build phase...
      - mvn clean package
  post_build:
    commands:
      - echo Build completed successfully
artifacts:
  files:
    - target/demo.jar
  discard-paths: yes


version: 0.2

phases:
  install:
    runtime-versions:
      java: openjdk8
    commands:
      - echo intall
  pre_build:
    commands:
      - echo pre_build
  build:
    commands:
      - cd project_2/demo
      - mvn package
      - echo build
  post_build:
    commands:
      - echo post_build
artifacts:
  files:
    - project_2/target/demo.jar
  discard-paths: yes